# Build hygiene — keep the image small and reproducible
**/__pycache__/
**/*.pyc
.git/
.gitignore
.aws-sam/
*.md
dashboard/run.sh

# Bundled sample CSVs are NOT baked into the image — the dashboard
# starts empty and is populated by a live benchmark or an upload.
dashboard/benchmark_data/*.csv
benchmark/*.csv
